CKEDITOR.editorConfig = function( config )
{
    // Define changes to default configuration here. For example:
    // config.uiColor = '#AADC6E';
    // config.skin = 'BootstrapCK-Skin,${createLink(uri: '/js/ckeditor/skins/BootstrapCK-Skin/')}';
    // config.height = 400;

    config.toolbar =
    [
	    ['Bold', 'Italic', 'Underline', '-', 'NumberedList', 'BulletedList', '-', 'Font','FontSize']
    ];

};